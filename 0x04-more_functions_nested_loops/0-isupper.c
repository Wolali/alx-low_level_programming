#include "main.h"

/**
 * _isupper - Checks for uppercase characters
 * @c: The number to be checked
 * Return: 1 for the uppercase or 0 for anything else
 */

int _isupper(int c)
{
	if (x >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
